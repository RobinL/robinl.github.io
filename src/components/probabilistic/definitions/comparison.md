In Splink, a model has a set of `comparisons`, each typically corresponding to a column in the input data such as name, date of birth etc.

Each comparison has multiple `comparison levels`. A `comparison level` defines a category of similarity for that column e.g. an 'exact match' or a 'one character difference'.

Each `comparison level` has an associated `partial match weight`.