A comparison level (scenario) is activated when its conditions are met for the pairwise record comparison at hand.  For instance, the exact match comparison level would be active for first name if the first names matched exactly.

Only one comparison level within each comparison can be active at any one time
